#include <iostream>

using namespace std;

int main() {
	char pre[100];

	while (cin.getline(pre, 100)) {
		for (int i = 0; i < 100; i++) {
			cout << pre[i] << endl;
			if (pre[i] == '\0')
				break;
		}
	}
	for (int i = 0; i < 100; i++) {
		switch (pre[i]) {
			case
		}
	}
}